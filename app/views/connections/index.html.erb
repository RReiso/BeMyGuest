<% provide(:title, 'Guests') %>
<section>
  <div class="container-fluid">
    <div class="row flex-nowrap">
      <%= render 'events/event_navbar' %>
      <div class="right">
        <% if @guests.blank? %>
          <p class="text-center mt-5">Guest list empty!</p>
        <% else %>
          <%= render 'connections/guest_table', locals: { guests: @guests } %>
        <% end %>
        <div class="d-flex justify-content-center">
          <%= link_to "Add guests", contact_list_path(@user,@event),  class: "border border-1 border-warning btn btn-sm btn-primary"%>
        </div>
      </div>
    </div>
  </div>
</section>
