<nav class="shadow py-0 navbar navbar-expand-sm navbar-dark bg-success ">
  <div class="container-fluid">
    <% if logged_in? %>
      <%= link_to "BeMyGuest", current_user, class: "px-1 nav-link brand-name" %>
    <% else %>
      <%= link_to "BeMyGuest", root_path, class: "px-1 nav-link brand-name" %>
    <% end %>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse ps-3 pe-1" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-1 mt-1 mb-lg-0 d-sm-flex justify-content-between">
        <% if logged_in? %>
        <div class="d-sm-flex">
          <li class="nav-item side-nav-link">
            <%=link_to "My events", current_user, class: "nav-link active",  "aria-current" => "page"%>
          </li>
          <li class="nav-item side-nav-link">
            <%= link_to "+ New","#", class: "nav-link active","aria-current" => "page", "data-bs-toggle"=>"modal", "data-bs-target"=>"#newEventModal", "data-bs-whatever"=>"@mdo" %>
          </li>
          <li class="nav-item side-nav-link">
            <%=link_to "Contacts", user_contacts_path(current_user), class: "nav-link active",  "aria-current" => "page"%>
          </li>
        </div>
        <div class="d-sm-flex justify-content-end">
          <li class="nav-item dropdown">
            <a class="nav-link side-nav-link dropdown-toggle active" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
              <span class="side-nav-link">
              <%= initials(current_user) %>
              </span>
            </a>
            <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="navbarDropdown">
              <li><%= link_to "Change password","#", class: "dropdown-item", "data-bs-toggle"=>"modal", "data-bs-target"=>"#passwordModal", "data-bs-whatever"=>"@mdo" %>
              </li>
              <li><hr class="dropdown-divider"></li>
              <li><%= link_to "Log out", logout_path, method: :delete, class: "dropdown-item" %></li>
            </ul>
          </li>
        </div>           
        <%= render "shared/change_password_modal" %>
        <% else %>
          <li class="nav-item w-100 d-flex justify-content-start justify-content-sm-end side-nav-link">
            <%=mail_to "rreiso@protonmail.com", "Contact Us",  class: "pb-1 nav-link active ",  "aria-current" => "page"%>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>