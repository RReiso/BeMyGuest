<div class="table-responsive object-table mb-1 mt-5">
  <table class="table mt-2 fs-6">
  <thead>
    <tr>
      <td colspan="5" class="fw-bold text-dark border border-info border-1 text-center p-1 bg-success">My events</td>
    </tr>
    <tr>
      <th>Event</th>
      <th>Date</th>
      <th>Time</th>
      <th>Place</th>
      <th></th>
    </tr>
  </thead>
  <tbody>
    <% @events.each do |event| %>
      <tr>
        <td><%=link_to event.name, user_event_path(@user,event), class: "link-to" %></td>
        <td><%= event.event_date.to_formatted_s(:rfc822) %></td>
        <td><%= event.event_time.to_formatted_s(:time) %></td>
        <td><%= event.place %></td>
        <td><%=link_to "", user_event_path(@user, event), method: :delete, data: {confirm: "Are you sure?"},"aria-label" => "Delete", class: " btn-close", type: :button%></td>
      </tr>
    <% end %>
  </tbody>
</table>
</div>
